+ **# Dlya debiliv #**
+ 
++This is Slaughterhouse.angular project.
++
+ git clone the repo
+ open dir
+ in console in project core: 
	use: npm cache clean
	then: npm install or start


# Install Globaly:
install node
npm install -g @angular/cli

# Install In folder:
npm install

# Run in folder
ng serve --open
ng serve --port=4201
